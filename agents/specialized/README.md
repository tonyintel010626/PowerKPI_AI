# agents/specialized

Domain-specific agents built on top of core agent logic.

## Examples for PowerKPI
- `kpi_analyst/`   — interprets KPI data, detects anomalies
- `report_builder/` — generates Power BI reports and visuals
- `data_connector/` — fetches and transforms data sources
