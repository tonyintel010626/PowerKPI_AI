# pipelines

Orchestration workflows that coordinate multiple agents and tools.
Pipelines are shared — users trigger them but don't own them.

## Examples for PowerKPI
- `daily_kpi_refresh`     — fetch, compute, and store KPI updates
- `report_generation`     — end-to-end report build workflow
- `anomaly_alert`         — detect anomaly and notify stakeholders
