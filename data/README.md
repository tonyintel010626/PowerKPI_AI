# data

Shared raw and processed data used by agents.
User-specific outputs go in memory/{username}/artifacts/ instead.

## Structure
- `raw/`        — unprocessed source data (CSVs, exports)
- `processed/`  — cleaned, normalized data ready for agents
- `samples/`    — small sample datasets for testing
