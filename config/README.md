# config

Shared configuration for the workspace.
Do NOT store secrets here — use environment variables or a secrets manager.

## Files
- `settings.yaml`       — global workspace settings
- `agents.yaml`         — agent registry and routing rules
- `tools.yaml`          — tool registry
- `logging.yaml`        — log levels and destinations
- `.env.example`        — template for required environment variables
